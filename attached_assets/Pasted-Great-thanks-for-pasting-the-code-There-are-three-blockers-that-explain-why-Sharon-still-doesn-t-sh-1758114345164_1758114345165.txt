Great—thanks for pasting the code. There are three blockers that explain why Sharon still doesn’t show “Partial Availability”:

Statuses aren’t canonicalized (so “Avail” never becomes “Available”)

You still have the || !availPairs.length fallback that forces “Other Unavailable”

You require a real statusAgg entry for the chosen status, but “Partial Availability” is a virtual label (so your guard suppresses the row)

Below are surgical diffs to fix all three.

1) Canonicalize statuses (so “Avail” → “Available”, typos tolerated)

Add this helper near your other utilities:

function canonicalStatus(raw: any): string {
  const s = String(raw ?? "").trim().toLowerCase();

  // available family
  if (s === "avail" || s.startsWith("avail")) return "Available";

  // time-killers
  if (s.startsWith("other unavail") || s.startsWith("othe")) return "Other Unavailable";
  if (s.includes("pre-agreed")) return "Pre-Agreed Appointment";

  // day-killers
  if (s.startsWith("holiday")) return "Holiday";
  if (s.startsWith("sick")) return "Sick";
  if (s.includes("maternity") || s.includes("paternity")) return "Maternity/Paternity";
  if (s.includes("compassion")) return "Compassionate Leave";

  if (s.includes("ad-hoc") || s.includes("adhoc")) return "Ad-hoc";
  return raw ?? "";
}


Use it when you build mergedData (this line is the big one):

- status: row.Type,
+ status: canonicalStatus(row.Type),


This guarantees your aggregation map (statusAgg) has keys like “Available” and “Other Unavailable”.

2) Don’t force “Other Unavailable” when availPairs is empty

In the selection block you currently have:

} else if (hasTimeKiller) {
  if (timeKillerIsAllDay || !availPairs.length) {
    highestPriorityStatus = "Other Unavailable";
  } else {
    highestPriorityStatus = "Partial Availability";
  }
}


Change it to remove the || !availPairs.length:

} else if (hasTimeKiller) {
- if (timeKillerIsAllDay || !availPairs.length) {
+ if (timeKillerIsAllDay) {
    highestPriorityStatus = "Other Unavailable";
  } else {
    highestPriorityStatus = "Partial Availability";
  }
}


With canonicalization (step 1), Sharon’s “Avail” rows feed into Available, so availPairs won’t be empty anyway. The OR clause was the main reason you kept falling into “Other Unavailable”.

Also make sure availPairs is built from the canonical key (you already do):

const availAgg = statusAgg.get("Available");
const availPairs = mergeIntervals(windowListToPairs(availAgg?.windows || []), 0);

3) Allow the virtual status to pass the guard

Right now you only push a record if that status exists in statusAgg:

if (highestPriorityStatus && statusAgg.has(highestPriorityStatus)) {
  const agg = statusAgg.get(highestPriorityStatus)!;
  ...
  cleanedRecords.push({...});
}


But “Partial Availability” will not exist in statusAgg (it’s a derived status). Replace that guard:

- if (highestPriorityStatus && statusAgg.has(highestPriorityStatus)) {
-   const agg = statusAgg.get(highestPriorityStatus)!;
+ if (highestPriorityStatus) {
+   const agg = statusAgg.get(highestPriorityStatus) ?? { hoursRaw: 0, windows: [], notes: [] };
    // ... (rest unchanged)


This lets you emit a row with status = "Partial Availability" while still using:

finalHours = Math.max(daily - totalLeaveCapped, 0.0);
netCapacity = finalHours;


…and the bookable windows come from Available – TIME_KILLERS (your code already does that right below).

(Nice-to-have) Add a one-off debug to confirm Sharon

Right after you build statusAgg for the day:

console.log("Statuses:", Array.from(statusAgg.keys()));
console.log("Available windows:", statusAgg.get("Available")?.windows);
console.log("Other Unavailable windows:", statusAgg.get("Other Unavailable")?.windows);
console.log({ hasDayKiller, hasTimeKiller, timeKillerIsAllDay });


Expected for Sharon (partial blocker days):

Statuses includes “Available” and “Other Unavailable”

timeKillerIsAllDay: false

Outcome after these three fixes

Sharon (partial Other Unavailable)
Status: Partial Availability
Windows: Available − blocker (each ≥ 60 min)
netCapacity: contractedDaily − capped leave (> 0)

Hamza (Other Unavailable all day)
Status: Other Unavailable
Windows: none
netCapacity: 0