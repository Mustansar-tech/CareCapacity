Normalize and merge overlapping or back-to-back intervals before any math.

Clip all times to the same day (00:00–24:00). Ignore invalid or empty intervals (end ≤ start).

unavailabilityMinutes counts only the portion that overlaps availability.

Workable time = Availability − Unavailability.

freeWindows = Workable − Scheduled, then:

Round starts up and ends down to roundToMinutes (default 15).

Drop windows shorter than minWindowMinutes (default 60).

differenceMinutes = desiredMinutes − unavailabilityMinutes − scheduledMinutes.

Return stable, deterministic ordering: sort rows by date then employeeId.

Also return dayRollups (aggregates for the top four tiles: Desired, Unavailability, Scheduled, Net).

Helper functions:

suggestStarts should propose concrete visit start/end pairs that fit inside given windows with optional grid step and buffers (before/after).

capacityInOneHourVisits returns how many 1-hour visits fit into the windows given optional buffers.

toHours1dp(90) → "1.5h".

Edge cases to handle:

Scheduled outside availability (shouldn’t create negative gaps; subtraction handles clipping).

Double bookings or adjacent slots (must merge first).

Unavailability partially overlapping a scheduled visit.

Employees/days present in only some inputs.

Output format: Provide the complete TypeScript file with the exports above and inline JSDoc. No external libraries. Keep it clean and readable.

Quick usage demo (include at the bottom as a commented example only):

Small sample arrays for one employee/day.

A call to computeCapacityWindows(...) with { minWindowMinutes: 60, roundToMinutes: 15 }.

Show (in comments) the resulting freeWindows and differenceMinutes for that day.

Fast follow prompts you can use after

“Adjust roundToMinutes to 5 and show me exactly which windows change in the demo.”

“Add a helper that returns only windows that can fit two back-to-back 60-min visits with a 10-min buffer between.”

“Create a React component EmployeeFreeWindows that shows a table like my screenshot (Desired/Unavailability/Scheduled/Diff) with a ‘View free windows’ popover per row, using toHours1dp. Tailwind only, no external UI libs.”

want me to paste a ready-made file with this logic into your repo instead?